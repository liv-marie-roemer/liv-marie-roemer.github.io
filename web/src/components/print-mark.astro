---
const { class: className, path = "", origin = "50, 50", width = "16" } = Astro.props;
const [originX, originY] = origin.split(",").map(Number);
---

<svg
  class={className}
  width={width}
  height="16"
  viewBox={`0 0 ${width} 16`}
  fill="none"
  style={`transform-origin: ${originX}% ${originY}%;`}
>
  <path d={path} stroke="black" stroke-width="1" />
</svg>

<style lang="scss">
  svg {
    overflow: visible;
  }
</style>
